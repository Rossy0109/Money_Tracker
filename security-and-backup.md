# নিরাপত্তা ও Google Drive ব্যাকআপ নীতি

## নিরাপত্তা ব্যবস্থা

অ্যাপটি Manus OAuth-ভিত্তিক সাইন-ইন ব্যবহার করে। সব আর্থিক API `protectedProcedure` দিয়ে সুরক্ষিত এবং প্রতিটি অ্যাকাউন্ট, ক্যাটাগরি, লেনদেন, বাজেট ও বিল রিমাইন্ডার রেকর্ডে authenticated `userId` প্রয়োগ করা হয়। সার্ভার প্রতিটি write, update ও delete অনুরোধে রেকর্ডের মালিকানা যাচাই করে; ক্লায়েন্ট কোনো `userId` পাঠাতে পারে না। ইনপুটগুলো সার্ভারে যাচাইকৃত, অর্থের পরিমাণ সসীম ও সীমাবদ্ধ, আর category/account সম্পর্কও একই ব্যবহারকারীর মধ্যে সীমাবদ্ধ।

## Google Drive-এর ভূমিকা

Google Drive **লাইভ ডেটাবেস নয়**। এটি source code বা ব্যবহারকারী-অনুমোদিত export-এর একটি আলাদা private backup location। লাইভ আর্থিক তথ্য ম্যানেজড ডেটাবেসে থাকে, যাতে ডেটা কোয়েরি, সমকালীন আপডেট, ব্যবহারকারী-আইসোলেশন এবং অ্যাপের লগইন নিয়ন্ত্রণ নির্ভরযোগ্যভাবে কাজ করে।

## প্রথম private backup

`My Hisab Secure Backups` নামের private Google Drive ফোল্ডার তৈরি করা হয়েছে। সেখানে `my-hisab-source-2026-08-19.zip` নামের source-code backup আপলোড করা হয়েছে। ফোল্ডার ও ফাইলটি শুধুমাত্র Drive account owner-এর জন্য উন্মুক্ত; নতুন কোনো sharing permission দেওয়া হয়নি। Archive-টি Git commit `804a5db` থেকে তৈরি এবং SHA-256 digest হলো `9364435b5d61126ba48b4f4f27a3e7d82f636a163cbbd790f10e02ef9c713e69`।

## কার্যকর সীমা

এই ওয়েব অ্যাপটি ব্যবহারকারীর ব্যক্তিগত Google connection সরাসরি ব্যবহার করে না। ফলে কোনো ব্যবহারকারীর অর্থনৈতিক ডেটা স্বয়ংক্রিয়ভাবে Drive-এ পাঠানো হয় না। অ্যাপের **নিজের ডেটা এক্সপোর্ট** বোতামটি কেবল authenticated ব্যবহারকারীর নিজের JSON ডেটা ডাউনলোড করে। ব্যবহারকারী চাইলে সেই ফাইল নিজে private Drive backup folder-এ আপলোড করবেন; export কোনো share-link তৈরি করে না।
