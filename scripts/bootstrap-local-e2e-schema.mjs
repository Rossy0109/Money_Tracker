import { createConnection } from "mysql2/promise";

const statements = [
  `CREATE TABLE users (
    id int AUTO_INCREMENT PRIMARY KEY,
    openId varchar(64) NOT NULL UNIQUE,
    name text,
    email varchar(320),
    passwordHash varchar(255),
    loginMethod varchar(64),
    role enum('user','admin','input_only') NOT NULL DEFAULT 'user',
    status enum('pending','active','suspended') NOT NULL DEFAULT 'pending',
    failedLoginAttempts int NOT NULL DEFAULT 0,
    lockedUntil timestamp NULL,
    resetToken varchar(255),
    resetTokenExpiresAt timestamp NULL,
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updatedAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    lastSignedIn timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP
  ) ENGINE=InnoDB`,
  `CREATE TABLE user_sessions (
    id int AUTO_INCREMENT PRIMARY KEY,
    userId int NOT NULL,
    sessionToken varchar(255) NOT NULL UNIQUE,
    refreshToken varchar(255) NOT NULL UNIQUE,
    userAgent text,
    ipAddress varchar(45),
    issuedAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    expiresAt timestamp NOT NULL,
    revokedAt timestamp NULL,
    lastUsedAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
  ) ENGINE=InnoDB`,
  `CREATE TABLE failed_login_attempts (
    id int AUTO_INCREMENT PRIMARY KEY,
    identifier varchar(320) NOT NULL,
    ipAddress varchar(45) NOT NULL,
    attemptCount int NOT NULL DEFAULT 1,
    firstAttemptAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    lastAttemptAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    lockedUntil timestamp NULL,
    UNIQUE KEY failed_login_attempts_identifier_ip_unique (identifier, ipAddress)
  ) ENGINE=InnoDB`,
  `CREATE TABLE login_history (
    id int AUTO_INCREMENT PRIMARY KEY,
    userId int NOT NULL,
    loginMethod varchar(64) NOT NULL,
    ipAddress varchar(45) NOT NULL,
    userAgent text,
    success tinyint(1) NOT NULL,
    failureReason varchar(255),
    attemptedAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP
  ) ENGINE=InnoDB`,
  `CREATE TABLE roles (
    id int AUTO_INCREMENT PRIMARY KEY,
    name varchar(64) NOT NULL UNIQUE,
    displayName varchar(120) NOT NULL,
    description text,
    isSystem tinyint(1) NOT NULL DEFAULT 0,
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updatedAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY roles_name_unique (name)
  ) ENGINE=InnoDB`,
  `CREATE TABLE permissions (
    id int AUTO_INCREMENT PRIMARY KEY,
    name varchar(128) NOT NULL UNIQUE,
    displayName varchar(128) NOT NULL,
    description text,
    category varchar(64) NOT NULL,
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updatedAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY permissions_name_unique (name)
  ) ENGINE=InnoDB`,
  `CREATE TABLE user_roles (
    id int AUTO_INCREMENT PRIMARY KEY,
    userId int NOT NULL,
    roleId int NOT NULL,
    assignedBy int NULL,
    assignedAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY user_roles_user_role_unique (userId, roleId)
  ) ENGINE=InnoDB`,
  `CREATE TABLE role_permissions (
    id int AUTO_INCREMENT PRIMARY KEY,
    roleId int NOT NULL,
    permissionId int NOT NULL,
    UNIQUE KEY role_permissions_role_permission_unique (roleId, permissionId)
  ) ENGINE=InnoDB`,
  `CREATE TABLE idempotency_keys (
    id int AUTO_INCREMENT PRIMARY KEY,
    userId int NOT NULL,
    idempotencyKey varchar(255) NOT NULL,
    route varchar(200) NOT NULL,
    requestHash varchar(64) NOT NULL,
    responseStatus int NOT NULL,
    responseBody text NOT NULL,
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    expiresAt timestamp NOT NULL,
    UNIQUE KEY idempotency_keys_user_key_unique (userId, idempotencyKey)
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_projects (
    id int AUTO_INCREMENT PRIMARY KEY,
    userId int NOT NULL,
    name varchar(120) NOT NULL,
    isActive tinyint(1) NOT NULL DEFAULT 1,
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updatedAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY finance_projects_user_name_unique (userId, name)
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_households (
    id int AUTO_INCREMENT PRIMARY KEY,
    ownerUserId int NOT NULL,
    name varchar(120) NOT NULL,
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updatedAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY finance_households_owner_name_unique (ownerUserId, name)
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_household_members (
    id int AUTO_INCREMENT PRIMARY KEY,
    householdId int NOT NULL,
    userId int,
    inviteeEmail varchar(320) NOT NULL,
    displayName varchar(120),
    role enum('editor','viewer') NOT NULL DEFAULT 'viewer',
    status enum('pending','active','declined','revoked') NOT NULL DEFAULT 'pending',
    invitedByUserId int NOT NULL,
    acceptedAt timestamp NULL,
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updatedAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY finance_household_member_email_unique (householdId, inviteeEmail),
    UNIQUE KEY finance_household_member_user_unique (householdId, userId)
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_shared_budgets (
    id int AUTO_INCREMENT PRIMARY KEY,
    householdId int NOT NULL,
    label varchar(120) NOT NULL,
    monthKey varchar(7) NOT NULL,
    amount decimal(18,2) NOT NULL,
    createdByUserId int NOT NULL,
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updatedAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY finance_shared_budgets_household_label_month_unique (householdId, label, monthKey)
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_shared_expenses (
    id int AUTO_INCREMENT PRIMARY KEY,
    householdId int NOT NULL,
    budgetId int NOT NULL,
    contributorUserId int NOT NULL,
    amount decimal(18,2) NOT NULL,
    note varchar(500),
    occurredAt timestamp NOT NULL,
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_accounts (
    id int AUTO_INCREMENT PRIMARY KEY,
    userId int NOT NULL,
    projectId int NOT NULL,
    name varchar(120) NOT NULL,
    type enum('cash','bank','mobile') NOT NULL,
    openingBalance decimal(18,2) NOT NULL DEFAULT 0.00,
    currentBalance decimal(18,2) NOT NULL DEFAULT 0.00,
    isActive tinyint(1) NOT NULL DEFAULT 1,
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updatedAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY finance_accounts_user_project_name_unique (userId, projectId, name)
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_categories (
    id int AUTO_INCREMENT PRIMARY KEY,
    userId int NOT NULL,
    projectId int NOT NULL,
    name varchar(120) NOT NULL,
    type enum('income','expense') NOT NULL,
    isDefault tinyint(1) NOT NULL DEFAULT 0,
    isActive tinyint(1) NOT NULL DEFAULT 1,
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY finance_categories_user_project_name_type_unique (userId, projectId, name, type)
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_recurring_transactions (
    id int AUTO_INCREMENT PRIMARY KEY,
    userId int NOT NULL,
    projectId int NOT NULL,
    accountId int NULL,
    categoryId int NOT NULL,
    type enum('income','expense') NOT NULL,
    amount decimal(18,2) NOT NULL,
    paymentMethod varchar(100) NOT NULL,
    note varchar(500),
    frequency enum('weekly','monthly') NOT NULL,
    scheduleDay int NOT NULL,
    nextRunAt timestamp NOT NULL,
    lastGeneratedAt timestamp NULL,
    isActive tinyint(1) NOT NULL DEFAULT 1,
    scheduleCronTaskUid varchar(65),
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updatedAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY finance_recurring_schedule_cron_task_unique (scheduleCronTaskUid)
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_transactions (
    id int AUTO_INCREMENT PRIMARY KEY,
    userId int NOT NULL,
    projectId int NOT NULL,
    accountId int NULL,
    categoryId int NOT NULL,
    type enum('income','expense') NOT NULL,
    amount decimal(18,2) NOT NULL,
    voucherNo varchar(80),
    reason varchar(180),
    paymentMethod varchar(100) NOT NULL,
    note varchar(500),
    recurringTemplateId int NULL,
    recurringRunKey varchar(16),
    occurredAt timestamp NOT NULL,
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY finance_transactions_recurring_run_unique (recurringTemplateId, recurringRunKey)
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_voucher_settings (
    id int AUTO_INCREMENT PRIMARY KEY,
    userId int NOT NULL,
    projectId int NOT NULL,
    prefix varchar(24) NOT NULL DEFAULT 'V',
    startNumber int NOT NULL DEFAULT 1,
    endNumber int NOT NULL DEFAULT 999999,
    nextNumber int NOT NULL DEFAULT 1,
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updatedAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY finance_voucher_settings_user_project_unique (userId, projectId)
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_budgets (
    id int AUTO_INCREMENT PRIMARY KEY,
    userId int NOT NULL,
    projectId int NOT NULL,
    categoryId int NOT NULL,
    monthKey varchar(7) NOT NULL,
    amount decimal(18,2) NOT NULL,
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updatedAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY finance_budgets_user_project_category_month_unique (userId, projectId, categoryId, monthKey)
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_bills (
    id int AUTO_INCREMENT PRIMARY KEY,
    userId int NOT NULL,
    projectId int NOT NULL,
    title varchar(180) NOT NULL,
    amount decimal(18,2) NOT NULL,
    dueAt timestamp NOT NULL,
    isPaid tinyint(1) NOT NULL DEFAULT 0,
    reminderDaysBefore int NOT NULL DEFAULT 3,
    lastReminderAt timestamp NULL,
    scheduleCronTaskUid varchar(65),
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updatedAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY finance_bills_schedule_cron_task_unique (scheduleCronTaskUid)
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_dues (
    id int AUTO_INCREMENT PRIMARY KEY,
    userId int NOT NULL,
    projectId int NOT NULL,
    type enum('debt','receivable') NOT NULL,
    counterparty varchar(180) NOT NULL,
    originalAmount decimal(18,2) NOT NULL,
    outstandingAmount decimal(18,2) NOT NULL,
    voucherNo varchar(80),
    reason varchar(180),
    note varchar(500),
    openedAt timestamp NOT NULL,
    dueAt timestamp NULL,
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_due_settlements (
    id int AUTO_INCREMENT PRIMARY KEY,
    userId int NOT NULL,
    projectId int NOT NULL,
    dueId int NOT NULL,
    accountId int NULL,
    amount decimal(18,2) NOT NULL,
    voucherNo varchar(80),
    note varchar(500),
    occurredAt timestamp NOT NULL,
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP
  ) ENGINE=InnoDB`,
  `CREATE TABLE audit_logs (
    id int AUTO_INCREMENT PRIMARY KEY,
    actorUserId int NOT NULL,
    actorRole varchar(20) NOT NULL DEFAULT 'user',
    projectId int NULL,
    action enum('create','update','delete','delete_attempt','approve','reject','post','reverse','login','logout','login_failed','permission_denied','user_suspended','backup_created','backup_restored') NOT NULL,
    entityType varchar(80) NOT NULL,
    entityId int NULL,
    summary varchar(300) NOT NULL,
    oldData json NULL,
    newData json NULL,
    ipAddress varchar(45),
    userAgent text,
    requestId varchar(64),
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_private_storage_objects (
    id int AUTO_INCREMENT PRIMARY KEY,
    ownerUserId int NOT NULL,
    projectId int NULL,
    householdId int NULL,
    storageKey varchar(512) NOT NULL UNIQUE,
    kind enum('backup','export') NOT NULL,
    scope enum('owner','household') NOT NULL,
    contentType varchar(160) NOT NULL,
    fileName varchar(255) NOT NULL,
    sizeBytes int NOT NULL,
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY finance_private_storage_key_unique (storageKey)
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_invoices (
    id int AUTO_INCREMENT PRIMARY KEY,
    userId int NOT NULL,
    projectId int NOT NULL,
    invoiceNumber varchar(64) NOT NULL,
    clientName varchar(160) NOT NULL,
    clientPhone varchar(40),
    clientEmail varchar(320),
    clientAddress text,
    clientBinTin varchar(64),
    issueDate timestamp NOT NULL,
    dueDate timestamp NOT NULL,
    subtotal decimal(18,2) NOT NULL,
    discountAmount decimal(18,2) NOT NULL DEFAULT 0.00,
    vatAmount decimal(18,2) NOT NULL DEFAULT 0.00,
    grandTotal decimal(18,2) NOT NULL,
    paidAmount decimal(18,2) NOT NULL DEFAULT 0.00,
    status enum('draft','unpaid','partially_paid','paid','overdue','cancelled') NOT NULL DEFAULT 'unpaid',
    notesTerms text,
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updatedAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY finance_invoices_project_number_unique (projectId, invoiceNumber)
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_invoice_items (
    id int AUTO_INCREMENT PRIMARY KEY,
    invoiceId int NOT NULL,
    description varchar(255) NOT NULL,
    quantity decimal(10,2) NOT NULL,
    unitPrice decimal(18,2) NOT NULL,
    vatRate decimal(5,2) NOT NULL DEFAULT 0.00,
    total decimal(18,2) NOT NULL,
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_inventory_items (
    id int AUTO_INCREMENT PRIMARY KEY,
    userId int NOT NULL,
    projectId int NOT NULL,
    name varchar(180) NOT NULL,
    sku varchar(80),
    category varchar(100),
    unit varchar(40) NOT NULL DEFAULT 'পিস',
    purchasePrice decimal(18,2) NOT NULL DEFAULT 0.00,
    sellingPrice decimal(18,2) NOT NULL DEFAULT 0.00,
    currentStock decimal(12,2) NOT NULL DEFAULT 0.00,
    lowStockThreshold decimal(12,2) NOT NULL DEFAULT 5.00,
    notes text,
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updatedAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_employees (
    id int AUTO_INCREMENT PRIMARY KEY,
    userId int NOT NULL,
    projectId int NOT NULL,
    name varchar(180) NOT NULL,
    phone varchar(40),
    email varchar(320),
    designation varchar(120),
    department varchar(120),
    joiningDate timestamp NULL,
    baseSalary decimal(18,2) NOT NULL DEFAULT 0.00,
    status enum('active','inactive','terminated') NOT NULL DEFAULT 'active',
    paymentMethod enum('cash','bank','mobile') NOT NULL DEFAULT 'cash',
    bankAccountDetails text,
    notes text,
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updatedAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_salary_payments (
    id int AUTO_INCREMENT PRIMARY KEY,
    userId int NOT NULL,
    projectId int NOT NULL,
    employeeId int NOT NULL,
    monthKey varchar(7) NOT NULL,
    baseSalary decimal(18,2) NOT NULL,
    bonusAmount decimal(18,2) NOT NULL DEFAULT 0.00,
    allowanceAmount decimal(18,2) NOT NULL DEFAULT 0.00,
    advanceDeduction decimal(18,2) NOT NULL DEFAULT 0.00,
    otherDeduction decimal(18,2) NOT NULL DEFAULT 0.00,
    netPayable decimal(18,2) NOT NULL,
    paidAmount decimal(18,2) NOT NULL DEFAULT 0.00,
    paymentDate timestamp NULL,
    accountId int NULL,
    voucherNo varchar(80),
    status enum('pending','paid','partially_paid') NOT NULL DEFAULT 'pending',
    notes text,
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updatedAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY finance_salary_employee_month_unique (projectId, employeeId, monthKey)
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_employee_advances (
    id int AUTO_INCREMENT PRIMARY KEY,
    userId int NOT NULL,
    projectId int NOT NULL,
    employeeId int NOT NULL,
    amount decimal(18,2) NOT NULL,
    repaidAmount decimal(18,2) NOT NULL DEFAULT 0.00,
    disbursedDate timestamp NOT NULL,
    accountId int NULL,
    voucherNo varchar(80),
    status enum('open','settled') NOT NULL DEFAULT 'open',
    notes text,
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updatedAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_vouchers (
    id int AUTO_INCREMENT PRIMARY KEY,
    userId int NOT NULL,
    projectId int NOT NULL,
    voucherNo varchar(80) NOT NULL,
    date timestamp NOT NULL,
    narration varchar(500),
    totalDebit decimal(18,2) NOT NULL DEFAULT 0.00,
    totalCredit decimal(18,2) NOT NULL DEFAULT 0.00,
    status enum('draft','submitted','approved','posted','reversed') NOT NULL DEFAULT 'draft',
    voucherType varchar(30) NOT NULL DEFAULT 'general',
    fiscalPeriodId int NULL,
    submittedBy int NULL,
    submittedAt timestamp NULL,
    approvedBy int NULL,
    approvedAt timestamp NULL,
    postedBy int NULL,
    postedAt timestamp NULL,
    reversedBy int NULL,
    reversedAt timestamp NULL,
    reversalReference varchar(120),
    isArchived tinyint(1) NOT NULL DEFAULT 0,
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updatedAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY finance_vouchers_project_voucher_unique (projectId, voucherNo)
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_voucher_debits (
    id int AUTO_INCREMENT PRIMARY KEY,
    voucherId int NOT NULL,
    accountId int NOT NULL,
    amount decimal(18,2) NOT NULL,
    narration varchar(300),
    sortOrder int NOT NULL DEFAULT 0
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_voucher_credits (
    id int AUTO_INCREMENT PRIMARY KEY,
    voucherId int NOT NULL,
    accountId int NOT NULL,
    amount decimal(18,2) NOT NULL,
    narration varchar(300),
    sortOrder int NOT NULL DEFAULT 0
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_ledger_entries (
    id int AUTO_INCREMENT PRIMARY KEY,
    voucherId int NOT NULL,
    accountId int NOT NULL,
    entryType enum('debit','credit') NOT NULL,
    amount decimal(18,2) NOT NULL,
    runningBalance decimal(18,2) NOT NULL,
    postedAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_journal_entries (
    id int AUTO_INCREMENT PRIMARY KEY,
    voucherId int NOT NULL,
    projectId int NOT NULL,
    journalNo varchar(80) NOT NULL,
    date timestamp NOT NULL,
    narration varchar(500),
    totalDebit decimal(18,2) NOT NULL,
    totalCredit decimal(18,2) NOT NULL,
    status enum('draft','posted','reversed') NOT NULL DEFAULT 'draft',
    postedBy int NULL,
    postedAt timestamp NULL,
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY finance_journal_entries_voucher_unique (voucherId),
    UNIQUE KEY finance_journal_entries_no_unique (projectId, journalNo)
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_journal_lines (
    id int AUTO_INCREMENT PRIMARY KEY,
    journalEntryId int NOT NULL,
    accountId int NOT NULL,
    entryType enum('debit','credit') NOT NULL,
    amount decimal(18,2) NOT NULL,
    narration varchar(300),
    sortOrder int NOT NULL DEFAULT 0,
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_voucher_references (
    id int AUTO_INCREMENT PRIMARY KEY,
    voucherId int NOT NULL,
    refType enum('cheque','bill','invoice','challan','other') NOT NULL,
    refNumber varchar(120) NOT NULL,
    refDate timestamp NULL,
    relatedEntityType varchar(50),
    relatedEntityId int NULL
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_voucher_audit (
    id int AUTO_INCREMENT PRIMARY KEY,
    voucherId int NOT NULL,
    actorUserId int NOT NULL,
    action enum('create','submit','approve','post','cancel','reverse') NOT NULL,
    snapshot json NOT NULL,
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_account_types (
    id int AUTO_INCREMENT PRIMARY KEY,
    code varchar(10) NOT NULL,
    name varchar(60) NOT NULL,
    nameBn varchar(60),
    normalBalance enum('debit','credit') NOT NULL,
    sortOrder int NOT NULL DEFAULT 0,
    isSystem tinyint(1) NOT NULL DEFAULT 0,
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY finance_account_types_code_unique (code)
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_chart_of_accounts (
    id int AUTO_INCREMENT PRIMARY KEY,
    userId int NOT NULL,
    projectId int NOT NULL,
    accountTypeId int NOT NULL,
    parentId int NULL,
    code varchar(30) NOT NULL,
    name varchar(120) NOT NULL,
    nameBn varchar(120),
    description varchar(500),
    isActive tinyint(1) NOT NULL DEFAULT 1,
    isDetail tinyint(1) NOT NULL DEFAULT 1,
    openingBalance decimal(18,2) NOT NULL DEFAULT 0.00,
    currentBalance decimal(18,2) NOT NULL DEFAULT 0.00,
    sortOrder int NOT NULL DEFAULT 0,
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updatedAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY finance_chart_of_accounts_project_code_unique (projectId, code)
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_account_groups (
    id int AUTO_INCREMENT PRIMARY KEY,
    userId int NOT NULL,
    projectId int NOT NULL,
    accountTypeId int NOT NULL,
    parentId int NULL,
    code varchar(20) NOT NULL,
    name varchar(120) NOT NULL,
    nameBn varchar(120),
    description varchar(500),
    sortOrder int NOT NULL DEFAULT 0,
    isSystem tinyint(1) NOT NULL DEFAULT 0,
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updatedAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY finance_account_groups_project_code_unique (projectId, code)
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_fiscal_periods (
    id int AUTO_INCREMENT PRIMARY KEY,
    userId int NOT NULL,
    projectId int NOT NULL,
    name varchar(120) NOT NULL,
    startDate timestamp NOT NULL,
    endDate timestamp NOT NULL,
    status enum('open','closed','locked') NOT NULL DEFAULT 'open',
    closedAt timestamp NULL,
    closedBy int NULL,
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updatedAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY finance_fiscal_periods_project_name_unique (projectId, name)
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_period_locks (
    id int AUTO_INCREMENT PRIMARY KEY,
    userId int NOT NULL,
    projectId int NOT NULL,
    monthKey varchar(7) NOT NULL,
    lockedAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    lockedBy int NOT NULL,
    reason varchar(300),
    UNIQUE KEY finance_period_locks_project_month_unique (projectId, monthKey)
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_voucher_reversals (
    id int AUTO_INCREMENT PRIMARY KEY,
    userId int NOT NULL,
    projectId int NOT NULL,
    originalVoucherId int NOT NULL,
    reversalVoucherId int NOT NULL,
    reason varchar(500) NOT NULL,
    reversedAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    reversedBy int NOT NULL,
    UNIQUE KEY finance_voucher_reversals_original_unique (originalVoucherId)
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_bank_reconciliations (
    id int AUTO_INCREMENT PRIMARY KEY,
    userId int NOT NULL,
    projectId int NOT NULL,
    accountId int NOT NULL,
    statementDate timestamp NOT NULL,
    statementBalance decimal(18,2) NOT NULL,
    bookBalance decimal(18,2) NOT NULL,
    difference decimal(18,2) NOT NULL DEFAULT 0.00,
    status enum('in_progress','completed','unreconciled') NOT NULL DEFAULT 'in_progress',
    reconciledAt timestamp NULL,
    reconciledBy int NULL,
    notes varchar(500),
    createdAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updatedAt timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
  ) ENGINE=InnoDB`,
  `CREATE TABLE finance_bank_reconciliation_items (
    id int AUTO_INCREMENT PRIMARY KEY,
    reconciliationId int NOT NULL,
    ledgerEntryId int NULL,
    statementRef varchar(120),
    statementDate timestamp NULL,
    statementAmount decimal(18,2),
    statementType enum('debit','credit'),
    matched tinyint(1) NOT NULL DEFAULT 0,
    matchedAt timestamp NULL,
    notes varchar(300)
  ) ENGINE=InnoDB`,
];

export async function bootstrapLocalE2eSchema(databaseUrl) {
  const connection = await createConnection(databaseUrl);
  try {
    for (const statement of statements) await connection.query(statement);
  } finally {
    connection.destroy();
  }
}